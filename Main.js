const firstDie = [1, 2, 3, 4, ,5 ,6];
const secondDie = firstDie

let rollDice = () => {
	firstDieValue = firstDie.Math(random);
	secondDieValue = secondDie.Math.(random);
	Return values[firstDieValue, secondDieValue];
}
